# NodeJS starter

## Installation

Run commands

```
cd client
npm install
npm run build
cd ..
npm install
npm run build
npm run start
```

To run for developing (with rerun after changing files):

```
npm run dev
```

Open [http://localhost:3080](http://localhost:3080)
